<%= link_to "Create a New Post", new_post_path, class:"btn btn-primary" %>

<% @posts.each do |post| %>
  <%= post.title %>
  <%= post.content %>
  <%= link_to post.content, posts_show_path(post) %>
<% end %>
